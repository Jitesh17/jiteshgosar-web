<script src="https://www.gstatic.com/dialogflow-console/fast/messenger/bootstrap.js?v=1"></script>
<df-messenger
  intent="WELCOME"
  chat-title="Jitesh’s Assistant"
  agent-id="5f61e638-87d6-4c62-8e8f-53d54b94623c"
  language-code="en"
  chat-icon="/images/Jitesh-photo-mini.png"
  storage-option="localStorage"
></df-messenger>

</style> --><style is:global>
  df-messenger {
    z-index: 999;

    /* --- THE INPUT AREA (White per request) --- */
    /* This creates a sharp contrast against the dark chat window */
    --df-messenger-input-box-color: #ffffff;
    --df-messenger-input-font-color: #101010;       /* Pitch black text for readability */
    --df-messenger-input-placeholder-font-color: #666666;

    /* --- HEADER & WINDOW (Deep Black) --- */
    --df-messenger-button-titlebar-color: #ffffff;
    --df-messenger-button-titlebar-font-color: #58667a;
    --df-messenger-chat-background-color: #11111100;  /* Matte Black */
    
    /* --- MESSAGES & TEXT --- */
    --df-messenger-font-color: #ffffff;             /* White text for the dark background */
    
    /* Bot: Dark Grey to blend in */
    --df-messenger-bot-message: #273744;            
    /* User: Lighter Grey to stand out */
    --df-messenger-user-message: #404040;           

    /* --- ICONS --- */
    --df-messenger-send-icon: #101010;              /* Black icon to match white input box */
    --df-messenger-minimized-chat-close-icon-color: #ffffff;
  }

  /* Structural Polish */
  df-messenger::part(chat-wrapper) {
    border: 1px solid #333; /* Keeps it defined on dark backgrounds */
    border-radius: 12px;
    box-shadow: 0 12px 40px rgba(0, 0, 0, 0.5);
  }
</style>